'use client';

import SmoothCursor from "../classes/SmoothCursor";

export default function EnableSmoothCursor ({ children }: { children: React.ReactNode }) {
    return <SmoothCursor children={children} />
}